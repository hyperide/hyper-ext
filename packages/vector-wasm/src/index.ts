/**
 * @file Vector WASM — public API
 *
 * Accessed via: import { PathOpsBackend, MockPathOps } from 'vector-wasm'
 */

export { MockPathOps } from './mock-pathops';
export type { BooleanOp, PathOpsBackend } from './types';
