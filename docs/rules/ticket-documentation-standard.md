# Ticket / task documentation standard

**Status: mandatory.** The full text of this standard now lives in the provisioned skill
`strict-ticket-discipline` (`~/.agents/skills/strict-ticket-discipline/SKILL.md`), which is
a strict superset of this file's six required sections (what's concretely wrong,
evidence-first, origin, consequence-if-unfixed, provable acceptance criteria, pseudocode)
plus five enforced documentation rules. Retired to this stub (HYP-949) to avoid drift.

Follow the skill. If `~/.agents/skills/` is not provisioned in your environment, run
`rig apply` (or your machine's provisioning step) to install it; this stub is the durable
pointer that keeps inbound links alive and tells you where the content went.
