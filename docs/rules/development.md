# Процесс разработки HyperIDE

**Статус: обязателен к исполнению.** Этот регламент описывает полный цикл работы над
изменением — от тикета до релиза. Нарушение этапов недопустимо (типичные нарушения
перечислены в конце). Применяется ко всем агентам (основной Claude, субагенты, Codex).

Связанные регламенты:

- [Запрос решения CTO](./cto-decision-requests.md) — когда и как эскалировать решение.

---

## Этап 0. Создание Linear-тикета

- Каждый баг / задача → отдельный тикет `HYP-XXX`.
- Ветки называются строго: `HYP-XXX-short-description`.
- **Без тикета — нет ветки, нет коммита.**

CLI: `linear issue create --team HYP --title "..." --description "..." --no-interactive`.

## Этап 1. Диагностика (root cause first)

По навыку `/systematic-debugging`:

- Читаем стектрейс / воспроизводим баг.
- Проверяем recent changes (`git log`, `git blame`).
- Собираем evidence послойно.
- **Не предлагаем фиксы, пока не найден root cause.**

## Этап 2. Реализация в worktree

- Worktree создаётся **только** в соседней папке `../hyper-canvas-draft-worktrees/`
  (sibling рядом с основным чекаутом). НЕ в `.claude/worktrees/` и НЕ внутри основного
  репозитория — это нарушение.
- **TDD обязателен:**
  1. Пишем failing-тест (красный).
  2. Запускаем — убеждаемся, что падает по нужной причине.
  3. Пишем минимальный код, чтобы тест прошёл.
  4. Тест зелёный — рефакторим при необходимости.

  Никогда: сначала код, потом тест.

- Тесты: `bun run test` (не `bun test` — тот цепляет `templates/`).
- Scope тестов: `client lib server shared vscode-extension`.
- Правки только через Edit/Write; **никаких `sed`/`perl`/`awk`** для редактирования файлов.
- **E2E живёт в ОТДЕЛЬНОМ репо `ext-test-projects/`** — клонируется **рядом** с этим
  репо (по умолчанию `../ext-test-projects/`, sibling-чекаут, НЕ внутри
  основного репо и не сабмодуль). Это неотъемлемая часть процесса, не внешний
  прожект. Поэтому `ext-test-projects/` и `bun run test:docker` НЕ найдутся из чистого
  клона одного основного репо — сначала склонируй ext-test-projects рядом. Прогон
  оттуда: `HYPER_E2E_SHARDS=1 bun run test:docker`.
- **Таймауты — зло.** Раздутые `timeout` / `poll interval` в e2e маскируют настоящий
  баг (флак, гонку, медленный старт). Запрещено «лечить» падение увеличением таймаута.
  Найти и починить root cause. Если таймаут поднят временно, чтобы разблокироваться —
  немедленно завести тикет на возврат и вернуть тесную границу, как только корень
  закрыт. Тайтовые границы держим осознанно.

## Этап 3. Перед коммитом

1. `bun run knip` — настроенный чек (не `bunx knip` — тот качает несконфигуренный
   и сыпет шум); убрать неиспользуемые экспорты / зависимости / файлы.
2. Self-review — TODO/FIXME, dead code, дубли, конвенции, comment hygiene
   (не осталось ли висящих JSDoc от удалённого кода).
3. Ревью — обязательный тандем, оба перед коммитом:
   - `advisor()` — сильный ревьюер (только главный агент, не сабагенты).
   - `review diff -C <repo>` (добавить `--staged`, если изменения уже застейджены) —
     дождаться до конца, прочитать findings.
4. Убедиться, что для новой логики есть тесты.
5. `/commit` — полный чеклист (Linear lookup → commit → пост-коммит).
   **Коммитим через `/commit`, не напрямую `git commit`.**

**Коммиты всегда проходят pre-commit хуки — `git commit --no-verify` / `-n` запрещён**
(см. [development → запрет --no-verify в AGENTS.md]). Если хук чёрнит кавычки — чинить
корень (`.oxfmtrc.jsonc` `singleQuote: true`), а не обходить хук.

**Адверсариальная проверка (доказательство от обратного).** Проверяя фикс или находку,
не подтверждай «работает» — попытайся СЛОМАТЬ. Построй случай, где оно ДОЛЖНО упасть;
ищи контрпример и реальные границы. Для утверждений «больше не происходит» / «всегда
работает» обязательна попытка опровержения: если контрпример нашёлся — реши, это
намеренная граница или баг; если не нашёлся — задокументируй, какие случаи проверены.

- **Конкретные пруфы, а не пересказ.** Пруф = артефакт (отрендеренный DOM,
  сгенерированный вывод, прогон теста, скриншот), а не «хелпер возвращает пусто» /
  «по построению верно». Проходи реальный путь (рендер / генерация), не только юнит-уровень.
- **Честные границы.** Явно назови, где фикс НЕ работает (floor / edge-кейсы) —
  задокументированная граница лучше ложного «универсально».

```text
// Утверждение: "после фикса <Badge/> рендерит children всегда"
// (1) Позитив — пруф через отрендеренный вывод, не через юнит:
render(<Badge>hi</Badge>)
assert dom.text == "hi"        // пруф = реальный DOM, не "хелпер вернул 'hi'"

// (2) От обратного — найти случай, где оно ОБЯЗАНО упасть:
render(<Badge>{null}</Badge>)
assert dom.text == ""          // контрпример: "всегда" опровергнут
                               // → решить: намеренная граница (floor) или баг
```

## Этап 4. Визуальная проверка (обязательно для UI)

- Скриншоты ДО и ПОСЛЕ через Playwright / `launchVSCode` (не `screencapture`).
- Проверяем: пустые панели, обрезанный контент, неправильные ширины / наложения,
  стейл-диалоги, неактивные табы.
- **Определение «готово»: баг НЕ считается готовым, пока серия скриншотов-пруфов
  не отправлена CTO в Telegram И CTO её не принял.** Зелёный e2e, мёрдж в main,
  «всё работает» — это НЕ «готово» без принятых CTO скриншотов. Никаких ✅ / «done»
  / «исправлено» до явного апрува скринов в TG. Правило действует на ВСЕ баги, в т. ч.
  уже влитые: если пруфов в TG не было — задача открыта, пока CTO не принял скрины.
- **Скрины ДО/ПОСЛЕ обязательно приложить ещё в двух местах — для долговечного
  пруфа:** (a) в описание / комментарий PR И (b) в Linear-тикет (комментарий или
  attachment). Принятия в Telegram НЕДОСТАТОЧНО: TG — эфемерный канал, пруф там
  не остаётся прикреплённым к коду. Долговечную запись несут именно PR и тикет, и
  оба обязаны хранить визуальный пруф. Для не-визуальных фиксов (поведение / тест)
  туда же кладётся RED/GREEN-сводка теста или вердикт адверсариальной проверки с
  явной пометкой, что это пруф поведения, а не скриншот.

## Этап 5. После коммита

- Комментарий в Linear с хешем коммита.
- Проверка stale docs (`AGENTS.md`, `.serena/memories/`).
- Deferred findings → Linear-тикеты немедленно.
- Security / API changes → `/security-review`.
- UI changes → `/frontend-design`.

## Этап 6. Мёрдж и релиз

- Cherry-pick или PR → `main`.
- `CHANGELOG` → bump version → tag.
- `bun run package` → `publish.sh`.
- TG с release notes.

---

## PR & ticket acceptance discipline

**Статус: обязателен к исполнению.** Раздел описывает, что обязано быть в каждом
PR и в каждом Linear-тикете, чтобы изменение можно было принять. Приёмка строится
на тех же артефактах, что и описание задачи — не дублируй, переиспользуй.

### Скриншоты — те же, что для приёмки

- PR **и** Linear-тикет **обязаны содержать скриншоты, когда это вообще возможно**.
  Это **те же самые** скриншоты, что используются для приёмки: сначала
  **«как есть» (before)**, затем **«как стало» (after)**.
- Before/after должны показывать **видимую разницу**. Один скриншот «как стало»
  без «как есть» — не пруф приёмки.
- Скриншоты UI снимаются через Playwright / `launchVSCode` (см. Этап 4), не
  `screencapture`.

### Схема — когда before/after неприменим или нужно «как нужно сделать»

- Если before/after-фото невозможно (docs / CI / чистый бэкенд), **или** нужно
  передать **«как нужно сделать»** (целевой стейт / дизайн, которого ещё нет) —
  приложи **схему**: SVG / mermaid / ASCII.
- Санкционированный способ сгенерировать схему — **codex** (проверено, работает):

  ```bash
  # валидный самодостаточный SVG:
  codex exec "Output ONLY a self-contained SVG diagramming X"
  ```

  codex также умеет выдавать **mermaid flowchart**. SVG/mermaid можно приложить
  напрямую. Если нужен растр (PNG) — отрендерь SVG через `@resvg/resvg-js`
  (зависимость `packages/vector-cli`).

### Мотивация и критерии приёмки

- Каждый PR/тикет **формулирует мотивацию / user value**, когда изменение завязано
  на user-facing value. Чисто инфраструктурное (docs / CI / рефактор) — так и
  пишем явно.
- Каждый PR/тикет перечисляет **явные критерии приёмки** (acceptance criteria)
  как task-list `- [ ]`.
- **Перед мёрджем ВСЕ критерии приёмки обязаны быть пройдены** (отмечены `- [x]`).

### Шаблон и автоматический гейт

- Шаблон PR — `.github/pull_request_template.md`: секции `## Motivation / user value`,
  `## Acceptance criteria`, `## Screenshots (before «как есть» → after «как стало»)`,
  опциональная `## Schematic (how it should work)`.
- Автоматический мёрдж-гейт — workflow `.github/workflows/pr-checklist-gate.yml`.
  У GitHub **нет** встроенного «блокировать мёрдж, пока не отмечены все чекбоксы в
  теле PR», поэтому гейт самописный: он парсит тело PR и **валит проверку, если
  остался хоть один незакрытый `- [ ]`** (проходит, когда все `- [x]` или чекбоксов
  нет). Логика парсера живёт в `.github/scripts/checklist-gate.mjs` (единый источник
  правды), её самопроверка — `bun .github/scripts/checklist-gate.test.mjs`.
- **Имя проверки (required check): `PR Checklist`.**
- **Tamper-устойчивость.** Гейт работает на триггере `pull_request_target`, поэтому и
  workflow, и парсер он берёт из **base-ветки (main)**, а не из самого PR. PR, который
  правит `.github/workflows/pr-checklist-gate.yml` или `.github/scripts/checklist-gate.*`,
  всё равно оценивается доверенной base-версией — изменённый в PR парсер не может
  «протащить» незакрытые чекбоксы. Гейт читает **только тело PR** из payload события
  (`context.payload.pull_request.body`); он **никогда не чекаутит и не выполняет код PR**
  (это и есть safety property `pull_request_target`). Собственный caveat `pull_request_target`:
  job получает write-токен и base-контекст — здесь это безопасно ровно потому, что мы
  не запускаем код PR; токен ограничен `permissions: contents: read`. Цена триггера:
  гейт **не запускается на PR, который его впервые вводит** (на main гейта ещё нет) —
  он «оживает» со следующего PR. Разбор: `docs/reviews/2026-06-04-pr-checklist-gate.md`.
- **Финальный шаг — только CTO (admin), не делается из репозитория.** Чтобы гейт
  стал ЖЁСТКИМ блоком мёрджа: Settings → Branches → правило защиты ветки `main` →
  «Require status checks to pass before merging» → добавить проверку с именем
  **`PR Checklist`**. Без этого тоггла гейт виден в PR, но не блокирует мёрдж
  технически.

---

## Типичные нарушения (не повторять)

- ✗ Worktrees создавались в `.claude/worktrees/` (должно: `-worktrees/`).
- ✗ Linear-тикеты не создавались перед работой.
- ✗ TDD не соблюдался (в т. ч. в субагентах).
- ✗ `bunx knip` не запускался.
- ✗ Визуальная проверка не делалась.
- ✗ `/commit` не вызывался (коммитили напрямую).
- ✗ Использовался `git commit --no-verify`.
- ✗ Файлы редактировались через `sed`/`perl`.
- ✗ PR/тикет без before/after-скриншотов (когда они были возможны).
- ✗ PR/тикет без мотивации и явных критериев приёмки.
- ✗ Мёрдж с незакрытыми критериями приёмки (`- [ ]`).
